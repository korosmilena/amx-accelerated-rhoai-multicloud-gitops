apiVersion: v1
kind: Service
metadata:
  name: {{ flask_app_app_name }}
  namespace: {{ flask_app_namespace_name }}
spec:
  selector:
    app: {{ flask_app_app_name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ flask_app_container_port }}
